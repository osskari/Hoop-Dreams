

module.exports = {
    queries: {
        allPlayers: () => [],
        player: (id) => ({})
    },
    mutations: {
        createPlayer: (player) => ({}),
        updatePlayer: (id, player) => ({}),
        removePlayer: (id) => ({})
    }
}